/shell3
/bin/
ai-do-not-read.*
# ./.shell3_project/ is runtime data — runs, inbox, sockets — never committed.
/.shell3_project/

# AI dev-process artifacts (kept local, not shipped publicly)
CLAUDE.md
docs/superpowers/
# Design specs / implementation plans + other AI working notes live in docs/dev
# locally but are never committed — only its README is tracked.
docs/dev/*
!docs/dev/README.md

# Build / test artifacts
*.out
*.prof
*.pprof
*.coverprofile
coverage.txt

# test coverage profile
cover.out

# stress-test scratch artifacts
/.stress_scratch
/tmp/

# stray scratch dir — never commit
asfd/
