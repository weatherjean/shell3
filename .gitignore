/shell3
/bin/
ai-do-not-read.*
# ./.shell3_project/ is runtime data — runs, inbox, sockets — never committed.
/.shell3_project/

# AI dev-process artifacts (kept local, not shipped publicly)
CLAUDE.md
docs/superpowers/
docs/dev/superpowers/

# Build / test artifacts
*.out
*.prof
*.pprof
*.coverprofile
coverage.txt

# test coverage profile
cover.out

# stress-test scratch artifacts
/.stress_scratch
/tmp/
