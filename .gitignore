/shell3
/bin/
# ./.shell3_project/ is runtime data — runs + subagent transcripts — never committed.
/.shell3_project/

# AI dev-process artifacts (kept local, not shipped publicly)
CLAUDE.md
docs/superpowers/
# Design specs, implementation plans, and other working notes stay local.
docs/dev/*

# Build / test artifacts
*.out
*.prof
*.pprof
*.coverprofile
coverage.txt

# subagent-driven-development scratch
.superpowers/
