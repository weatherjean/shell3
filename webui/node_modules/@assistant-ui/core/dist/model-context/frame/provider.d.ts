import { Unsubscribe } from "../../types/unsubscribe.js";
import { ModelContextProvider } from "../types.js";
//#region src/model-context/frame/provider.d.ts
declare class AssistantFrameProvider {
  private static _instance;
  private _providers;
  private _providerUnsubscribes;
  private _targetOrigin;
  private constructor();
  private static getInstance;
  private handleMessage;
  private handleToolCall;
  private sendMessage;
  private getModelContext;
  private broadcastUpdate;
  static addModelContextProvider(provider: ModelContextProvider, targetOrigin?: string): Unsubscribe;
  static dispose(): void;
}
//#endregion
export { AssistantFrameProvider };
//# sourceMappingURL=provider.d.ts.map