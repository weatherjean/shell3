import { ThreadMessage } from "../../../types/message.js";
import { ThreadPrimitiveMessages } from "../thread/ThreadMessages.js";
import { FC, ReactNode } from "react";
//#region src/react/primitives/part/PartMessages.d.ts
declare namespace PartPrimitiveMessages {
  type Props = {
    components: NonNullable<ThreadPrimitiveMessages.Props["components"]>;
    children?: never;
  } | {
    /** Render function called for each message. Receives the message. */
    children: (value: {
      message: ThreadMessage;
    }) => ReactNode;
    components?: never;
  };
}
/**
 * Renders the nested messages of a tool call part (e.g. sub-agent conversation).
 *
 * This primitive reads `messages` from the current tool call part in the PartScope
 * and renders them using a readonly thread context. All existing message and part
 * primitives work inside, and parent tool UI registrations are inherited.
 *
 * @example
 * ```tsx
 * const toolkit = defineToolkit({
 *   invoke_sub_agent: {
 *     type: "backend",
 *     render: () => (
 *       <PartPrimitive.Messages>
 *         {({ message }) => {
 *           if (message.role === "user") return <MyUserMessage />;
 *           return <MyAssistantMessage />;
 *         }}
 *       </PartPrimitive.Messages>
 *     ),
 *   },
 * });
 * ```
 */
declare const PartPrimitiveMessagesImpl: FC<PartPrimitiveMessages.Props>;
declare const PartPrimitiveMessages: import("react").NamedExoticComponent<PartPrimitiveMessages.Props>;
//#endregion
export { PartPrimitiveMessages, PartPrimitiveMessagesImpl };
//# sourceMappingURL=PartMessages.d.ts.map