import { MessagePartStatus, ToolCallMessagePartStatus } from "../../types/message.js";
import { PartMethods, PartState } from "./part.js";
//#region src/store/scopes/chain-of-thought.d.ts
type ChainOfThoughtPart = Extract<PartState, {
  type: "tool-call";
} | {
  type: "reasoning";
}>;
type ChainOfThoughtState = {
  readonly parts: readonly ChainOfThoughtPart[];
  readonly collapsed: boolean;
  readonly status: MessagePartStatus | ToolCallMessagePartStatus;
};
type ChainOfThoughtMethods = {
  /**
   * Get the current state of the chain of thought.
   */
  getState(): ChainOfThoughtState;
  /**
   * Set the collapsed state of the chain of thought accordion.
   */
  setCollapsed(collapsed: boolean): void;
  /**
   * Get the part methods for a specific part within this chain of thought.
   */
  part(selector: {
    index: number;
  }): PartMethods;
};
type ChainOfThoughtMeta = {
  source: "message";
  query: {
    type: "chainOfThought";
  };
};
type ChainOfThoughtClientSchema = {
  methods: ChainOfThoughtMethods;
  meta: ChainOfThoughtMeta;
};
//#endregion
export { ChainOfThoughtClientSchema, ChainOfThoughtMeta, ChainOfThoughtMethods, ChainOfThoughtPart, ChainOfThoughtState };
//# sourceMappingURL=chain-of-thought.d.ts.map