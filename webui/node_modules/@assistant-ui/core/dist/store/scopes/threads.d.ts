import { ThreadListItemMethods, ThreadListItemState } from "./thread-list-item.js";
import { ThreadMethods, ThreadState } from "./thread.js";
import { AssistantRuntime } from "../../runtime/api/assistant-runtime.js";
//#region src/store/scopes/threads.d.ts
type ThreadsState = {
  readonly mainThreadId: string;
  readonly newThreadId: string | null;
  readonly isLoading: boolean;
  readonly isLoadingMore: boolean;
  readonly hasMore: boolean;
  readonly threadIds: readonly string[];
  readonly archivedThreadIds: readonly string[];
  readonly threadItems: readonly ThreadListItemState[];
  readonly main: ThreadState;
};
type ThreadsMethods = {
  getState(): ThreadsState;
  switchToThread(threadId: string, options?: {
    unarchive?: boolean;
  }): void;
  switchToNewThread(): void;
  item(threadIdOrOptions: "main" | {
    id: string;
  } | {
    index: number;
    archived?: boolean;
  }): ThreadListItemMethods;
  thread(selector: "main"): ThreadMethods;
  getLoadThreadsPromise(): Promise<void>;
  reload(): Promise<void>;
  loadMore(): Promise<void>;
  __internal_getAssistantRuntime?(): AssistantRuntime;
};
type ThreadsClientSchema = {
  methods: ThreadsMethods;
};
//#endregion
export { ThreadsClientSchema, ThreadsMethods, ThreadsState };
//# sourceMappingURL=threads.d.ts.map