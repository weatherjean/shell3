import { ThreadListItemStatus } from "../../runtime/interfaces/thread-list-runtime-core.js";
import { ThreadListItemRuntime } from "../../runtime/api/thread-list-item-runtime.js";
//#region src/store/scopes/thread-list-item.d.ts
type ThreadListItemState = {
  readonly id: string;
  readonly remoteId: string | undefined;
  readonly externalId: string | undefined;
  readonly title?: string | undefined;
  readonly lastMessageAt?: Date | undefined;
  readonly status: ThreadListItemStatus;
  readonly custom?: Record<string, unknown> | undefined;
};
type ThreadListItemMethods = {
  getState(): ThreadListItemState;
  switchTo(options?: {
    unarchive?: boolean;
  }): void;
  rename(newTitle: string): void;
  updateCustom(custom: Record<string, unknown> | undefined): void;
  archive(): void;
  unarchive(): void;
  delete(): void;
  generateTitle(): void;
  initialize(): Promise<{
    remoteId: string;
    externalId: string | undefined;
  }>;
  detach(): void;
  __internal_getRuntime?(): ThreadListItemRuntime;
};
type ThreadListItemMeta = {
  source: "threads";
  query: {
    type: "main";
  } | {
    type: "id";
    id: string;
  } | {
    type: "index";
    index: number;
    archived?: boolean;
  };
};
type ThreadListItemEvents = {
  /**
   * @deprecated State-derivable. Compare `s.threads.mainThreadId` against the
   * item's `s.threadListItem.id` via `useAuiState` instead. Kept for backward
   * compatibility.
   */
  "threadListItem.switchedTo": {
    threadId: string;
  };
  /**
   * @deprecated State-derivable. Compare `s.threads.mainThreadId` against the
   * item's `s.threadListItem.id` via `useAuiState` instead. Kept for backward
   * compatibility.
   */
  "threadListItem.switchedAway": {
    threadId: string;
  };
};
type ThreadListItemClientSchema = {
  methods: ThreadListItemMethods;
  meta: ThreadListItemMeta;
  events: ThreadListItemEvents;
};
//#endregion
export { ThreadListItemClientSchema, ThreadListItemEvents, ThreadListItemMeta, ThreadListItemMethods, ThreadListItemState };
//# sourceMappingURL=thread-list-item.d.ts.map