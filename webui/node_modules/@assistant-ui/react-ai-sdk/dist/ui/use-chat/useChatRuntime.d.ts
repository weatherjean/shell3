import { AISDKRuntimeAdapter, CustomToCreateMessageFunction } from "./useAISDKRuntime.js";
import { ChatInit } from "ai";
import { AssistantRuntime, ExternalStoreSharedOptions } from "@assistant-ui/core";
import { UIMessage as UIMessage$1 } from "@ai-sdk/react";
import { AssistantCloud } from "assistant-cloud";
//#region src/ui/use-chat/useChatRuntime.d.ts
type UseChatRuntimeOptions<UI_MESSAGE extends UIMessage$1 = UIMessage$1> = ChatInit<UI_MESSAGE> & ExternalStoreSharedOptions & {
  cloud?: AssistantCloud | undefined;
  adapters?: AISDKRuntimeAdapter["adapters"] | undefined;
  toCreateMessage?: CustomToCreateMessageFunction;
  onResume?: AISDKRuntimeAdapter["onResume"];
  onResumeToolCall?: AISDKRuntimeAdapter["onResumeToolCall"];
  /**
   * Called when `useChatRuntime` automatically attempts to resume a pending
   * resumable stream on mount and that reconnect fails. Use this to surface
   * a toast, report telemetry, or mark the thread as needing a retry. The
   * stored stream id is still cleared after the callback runs.
   */
  onResumeError?: ((error: unknown) => void) | undefined;
  joinStrategy?: AISDKRuntimeAdapter["joinStrategy"];
  onThreadIdChange?: ((threadId: string | undefined) => void) | undefined;
};
declare const useChatRuntime: <UI_MESSAGE extends UIMessage$1 = UIMessage$1>({ cloud, onThreadIdChange, ...options }?: UseChatRuntimeOptions<UI_MESSAGE>) => AssistantRuntime;
//#endregion
export { UseChatRuntimeOptions, useChatRuntime };
//# sourceMappingURL=useChatRuntime.d.ts.map