version: 2

project_name: shell3

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/shell3
    binary: shell3
    env:
      - CGO_ENABLED=0
    # Unix-only by design (process groups, TTY handling); see README.
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - formats: [tar.gz]
    # Deterministic, lowercase os/arch names so install.sh can build the asset
    # URL without querying the asset list. e.g. shell3_0.0.1_linux_amd64.tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md

checksum:
  name_template: checksums.txt

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs"
      - "^test"
      - "^ci"
      - "^chore"

release:
  draft: true # review the generated notes before publishing
